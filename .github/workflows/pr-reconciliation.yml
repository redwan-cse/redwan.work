name: PR reconciliation evidence (retired)
on:
  push:
    branches: [fix/direct-public-asset-uploads]
    paths: [.github/workflows/pr-reconciliation.yml]
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Exact ancestry and dependency evidence captured; status-write capability retired.'
