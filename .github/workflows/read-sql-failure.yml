name: Prior SQL diagnostic retired
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Prior SQL failure isolated; diagnostic API and status-write access retired.'
