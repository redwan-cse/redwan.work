name: Release evidence capture retired
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Release evidence recorded; diagnostic status-write access retired.'
