name: Connect reviewed project contracts
on:
  push:
    branches: [fix/direct-public-asset-uploads]
    paths:
      - .github/scripts/finish-project-contracts.py
      - .github/workflows/finish-project-contracts.yml
permissions:
  contents: write
jobs:
  integrate:
    if: github.repository == 'redwan-cse/redwan.work' && github.ref == 'refs/heads/fix/direct-public-asset-uploads'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
      - name: Apply exact inspected two-file edit
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 .github/scripts/finish-project-contracts.py
