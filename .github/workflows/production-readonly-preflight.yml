name: Production preflight access retired
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Read-only preflight completed. Production environment and secret mappings removed; see the redacted evidence document.'
