name: Review evidence capture completed
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  evidence:
    runs-on: ubuntu-latest
    steps:
      - name: Completed one-time diagnostic collection
        run: echo 'Evidence preserved in docs/security/REVIEW-EVIDENCE-CAPTURE.md. No log access, status writing or extra repository permissions remain in this workflow.'
