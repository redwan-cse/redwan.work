name: PR sequence evidence retired
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Read-only sequence inspection completed; temporary status publishing retired.'
