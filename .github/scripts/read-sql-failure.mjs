// Prior failure was classified safely; no API calls or status writes remain.
console.log('Prior SQL failure was a fixture using direct payment insertion instead of the protected RPC. Fixture corrected.');
