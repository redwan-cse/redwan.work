import assert from 'node:assert/strict';import test from 'node:test';import {isDirectBlogImage} from '../../lib/blog-image-policy.ts';
test('only exact HTTPS image hosts bypass optimization',()=>{for(const url of ['https://cdn.jsdelivr.net/gh/example/image.png','https://raw.githubusercontent.com/example/image.png','https://CDN.JSDELIVR.NET/image.png'])assert.equal(isDirectBlogImage(url),true);for(const url of ['https://cdn.jsdelivr.net.attacker.test/a','https://attacker.test/cdn.jsdelivr.net/a','https://attacker.test/?src=githubusercontent.com','https://cdn.jsdelivr.net@attacker.test/a','https://user:secret@cdn.jsdelivr.net/a','http://cdn.jsdelivr.net/a','https://cdn.jsdelivr.net:444/a','//cdn.jsdelivr.net/a','not a url',null])assert.equal(isDirectBlogImage(url),false);});
