import assert from 'node:assert/strict';import test from 'node:test';import {parseMilestoneMoney} from '../../lib/crm/milestone-money.ts';
test('decimal amounts use exact cents without rounding extra precision',()=>{for(let i=0;i<=10000;i++){const amount=(i/100).toFixed(2);assert.deepEqual(parseMilestoneMoney('',amount),{ok:true,amount_cents:i});}assert.deepEqual(parseMilestoneMoney('2147483647',''),{ok:true,amount_cents:2147483647});assert.deepEqual(parseMilestoneMoney('','21474836.47'),{ok:true,amount_cents:2147483647});assert.deepEqual(parseMilestoneMoney('',''),{ok:true});});
test('ambiguous, fractional cents, exponent, malformed and overflow amounts refuse',()=>{for(const [cents,amount] of [['1','1'],['1.1',''],['','1.005'],['','-1'],['','1e2'],['','Infinity'],['','NaN'],['','21474836.48'],['2147483648',''],['','0x10'],[null,'1'],['',1]])assert.deepEqual(parseMilestoneMoney(cents,amount),{ok:false});});
