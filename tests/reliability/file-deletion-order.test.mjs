import assert from 'node:assert/strict';import test from 'node:test';import {registerHooks} from 'node:module';
const id='11111111-1111-4111-8111-111111111111',actor='22222222-2222-4222-8222-222222222222';
const f={physical:0,prepared:0};globalThis.__deletionOrder=f;
const modules={'server-only':'export {};','@/lib/crm/result':'export function crmError(error){return {ok:false,error};}', '@/lib/supabase/admin':`export function getSupabaseAdmin(){return {async rpc(){globalThis.__deletionOrder.prepared++;return {data:null,error:{message:'synthetic refusal'}};},from(table){const q={select(){return q;},eq(){return q;},delete(){return q;},async maybeSingle(){return {data:table==='profiles'?{role:'admin',is_active:true}:{id:'${id}',bucket:'private',kind:'deliverable',r2_key:'private/synthetic',uploaded_by:'${actor}'},error:null};},then(a,b){return Promise.resolve({data:null,error:{message:'synthetic DB refusal'}}).then(a,b);}};return q;}};}`, '@/lib/r2':'export function isPortalKey(){return true;}export function validateContactFile(){return {ok:true};}export async function presignPrivateGet(){}export async function deletePrivateObjects(){globalThis.__deletionOrder.physical++;}'};
const hooks=registerHooks({resolve(s,c,n){if(Object.hasOwn(modules,s))return {url:'data:text/javascript,'+encodeURIComponent(modules[s]),shortCircuit:true};if(s==='@/lib/crm/file-deletion')return {url:new URL('../../lib/crm/file-deletion.ts',import.meta.url).href,shortCircuit:true};return n(s,c);}});const {deleteOwnedFile}=await import('../../lib/crm/files.ts');hooks.deregister();
test('refused durable preparation must never delete object bytes',async()=>{assert.equal((await deleteOwnedFile(id,{userId:actor,role:'admin'})).ok,false);assert.equal(f.physical,0,'Object bytes were deleted before database preparation committed');assert.equal(f.prepared,1);});
