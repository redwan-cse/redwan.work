import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import test from 'node:test';
test('invoice server page does not invoke a client-exported formatter',()=>{const source=readFileSync(new URL('../../app/(admin)/admin/invoices/[id]/page.tsx',import.meta.url),'utf8');const imported=source.match(/import\s*\{([^}]+)\}\s*from\s*['"]@\/components\/admin\/invoice-forms['"]/);assert.ok(imported);assert.equal(imported[1].split(',').map(v=>v.trim()).includes('money'),false);assert.ok(source.includes('function money('));assert.ok(source.includes("workflowSession('admin')"));});
