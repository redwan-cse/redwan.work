# Read-only investigation of unsuccessful Auth correction

Owner September9 at11:54 Asia/Dhaka approved investigation only, explicitly no production write retry. The historical mutation workflow remains retired. New diagnostic performs exactly two GETs to the same project's Management Auth config, three seconds apart; no production PATCH/POST/PUT/DELETE, SQL, user/mail/token or cron calls. Only finite classifications leave memory. TLS verification, fixed host/path, bounded512KiB response,15-second timeout, no redirects, isolated pinned source/no package installs, test-before-secrets and credential-free GitHub reporting apply. Temporary finite-result file is mode0600 and deleted; no raw config or secret artifact.

## Proven reporting defect, not proven provider root cause

The exact historical source296ed616abdb0d43e81d32fa0ff2caed472862a6 drops the PATCH status, readback mismatch category and all caught-error stages before publishing ROLLED_BACK. Locally executed synthetic cases using that code reproduced: (1) PATCH403/unchanged values -> ROLLED_BACK with one attempted fixture write; (2) successful PATCH but unrelated metadata drift -> ROLLED_BACK with a second fixture restoration; (3) successful response with unchanged immediate readback -> ROLLED_BACK with one fixture write. No actual network/prod mutation was used in these reproductions. This establishes ambiguity, not which scenario happened in production.

Current official docs confirm PATCH success200 and optional site_url/mailer_templates_recovery_content fields: https://supabase.com/docs/reference/api/v1-update-auth-service-config . So the success200 requirement is documented, not a demonstrated response-code bug. Read permission does not establish auth write permission. No permission was escalated/tested via writes.

Historical public Actions log fetch did not return content. The pinned source logs only the final category, not the discarded transport/mismatch details. A fresh read can establish current state and whether repeated GETs vary now; it cannot reconstruct the old HTTP status or prove past transient/provider/concurrent changes. Never promote STABLE now into a historical-cause claim. The before/after comparator checks all returned config fields, which could trigger rollback on a provider-managed field, but that remains a hypothesis until observed.

Next diagnostic output remains pending. Do not retry production or relax checks based on this document. Any future correction design should retain finite HTTP/stage/target-equality/unrelated-drift and actual rollback-attempt/verification categories without exposing values. Separate delayed-read handling from denied/ambiguous transport, and preserve concurrent-edit refusal. No implementation or production rerun of that redesigned writer is included in this approval.
