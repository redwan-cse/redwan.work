# Approved production Auth origin/recovery correction

Owner approval: September9 2026,11:37 Asia/Dhaka: fix ONLY production Auth origin and recovery template. This is not permission to merge, migrate, activate Cron, change R2, send email or alter unrelated settings.

Implementation uses documented PATCH /v1/projects/{ref}/config/auth with exactly site_url and mailer_templates_recovery_content. Reference: https://supabase.com/docs/reference/api/v1-update-auth-service-config . Set site_url=https://redwan.work. Replace exactly one existing href containing {{ .ConfirmationURL }} with {{ .SiteURL }}/reset-password?token_hash={{ .TokenHash }}&amp;type=recovery; preserve all presentation, text and unrelated links. Refuse ambiguous/unrecognized templates. Main27ab3e3f1895b0d81231ddf1b83174fc24ad1f8a app/(auth)/reset-password/page.tsx already consumes token_hash; no application deployment is required for this link contract.

Inspect full Auth config privately in memory, save only the original two fields in exclusive mode0600 temporary rollback JSON, then re-read config and refuse if it changed. PATCH only the two fields. Read back and compare desired two values and every returned unrelated config value. No automatic write retry. On failure, guarded rollback restores only the original two fields if their current values remain original/ours; never overwrite a concurrent third-party edit. A read/check/write gap is not atomic CAS and process loss can defeat automatic recovery, so no absolute concurrency/crash guarantee is claimed. The private snapshot exists only for the job and is removed during cleanup, not uploaded as an artifact or retained as a customer-data backup.

Use a pinned built-in-only Node script in isolated GitHub Environment Production. Only Supabase Management token and project URL are mapped on the apply step; no application key, Resend, R2, database or Cron credential. Eight synthetic tests run without credentials before applying. TLS verification explicit, response512KiB bound,15-second timeout, fixed API hostname/path, no redirects/raw provider output. Separate GitHub-only reporter publishes one allowlisted outcome. A failure/outstanding-private-review result is not successful verification. No real-user/email/token operations are part of this action.

Eight synthetic tests passed locally: presentation preservation, strict two-key/path boundary, readback/unrelated settings, idempotence, concurrent-change refusal, snapshot failure, partial rollback, unrelated-drift detection, third-party rollback refusal and sanitized errors. These are functional/negative tests, not an independently executed review or historical TDD-red claim. Actual file execution repeats in pinned Actions. Source and workflow must be retired after the one-shot correction; preserve sanitized exact-head result in the existing PR tracker.
